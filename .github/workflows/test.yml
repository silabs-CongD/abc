name: test

on:
  push

env:
  HAHA: ${{ vars.PUBLIC_ENV }}
jobs:
  test:
    if: ${{ vars.PUBLIC_ENV  == 'public' }}
    runs-on: ubuntu-latest
    steps:
      - name: test
        run: |
          echo $HAHA
          
          echo ${{ vars.PUBLIC_ENV }}
